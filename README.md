# Gadfly Server (Backend)

This is the Node.js backend for the Gadfly dating application.
It supports:

- Local JSON Database (for testing)
- MongoDB Connection (for Cloud)
- Admin Panel API
- Mobile OTP Login

## Setup

1. `npm install`
2. `node server.js`
